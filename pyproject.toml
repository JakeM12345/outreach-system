[project]
name = "outreach-system"
version = "0.1.0"
description = "Automated outreach: Google Places scraper + Gmail SMTP sender"
requires-python = ">=3.12"
dependencies = [
    "requests==2.31.0",
    "python-dotenv==1.0.0",
]
